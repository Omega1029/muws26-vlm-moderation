# MUWS26 — efficient multimodal toxic-content detection
# Install into ./venv (Python 3.10). CUDA 12.x / A100.
# cu121 build — matches this box's driver (535 / CUDA 12.2). torch>=2.5 ships cu124+ and won't init here.
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
transformers==4.51.3        # Qwen2-VL + SmolVLM (Idefics3); pinned <5 (5.x dropped Idefics3 image-proc registration)
accelerate>=0.34
bitsandbytes>=0.43          # int8 / int4 (nf4) load-time quantization
peft>=0.13                  # LoRA
datasets>=2.20
scikit-learn>=1.3           # AUROC / F1
Pillow>=10.0
PyYAML>=6.0
matplotlib>=3.8
# qwen2-vl image preprocessing helper (optional but recommended for the anchor):
qwen-vl-utils
